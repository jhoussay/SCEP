INCLUDEPATH *= $$PWD/include

include($$PWD/../../VERSION)

LIBS *= -lUxTheme -lShlwapi
build_pass:CONFIG(debug, debug|release) {
    LIBS *= -lSCEP_CORE_d$$SCEP_MAJ_VERSION
} else {
    LIBS *= -lSCEP_CORE$$SCEP_MAJ_VERSION
}
